  <h1 class="sr-only">
      Crystal Maidens Helper
  </h1>
  <nav class="navbar navbar-expand-xl navbar-light bg-light">
      <div class="container mt-4 mb-3">
          <a class="navbar-brand mr-4 pr-2" href="{{ path('homepage') }}">
              Crystal Maidens Helper
          </a>
          <button class="navbar-toggler border-0" type="button" data-toggle="collapse" data-target="#header-menu" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Show/Hide navigation">
              <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="header-menu">
              <ul class="navbar-nav ml-auto">
                  {% if app.user %}
                  {% if is_granted('ROLE_ADMIN') %}
                  <li class="nav-item mr-3">
                      <a class="nav-link" href="{{ path('admin') }}">
                          Admin
                      </a>
                  </li>
                  {% endif %}
                  <li class="nav-item mr-3">
                      <a class="nav-link" href="{{ path('app_logout') }}" ><span class="bi bi-person-circle"></span> Logout</a>
                  </li>
                  {% else %}
                  <li class="nav-item mr-3">
                      <a class="nav-link" href="{{ path('app_login') }}" ><span class="bi bi-box-arrow-in-right"></span> Login</a>
                  </li>
                  <li class="nav-item mr-3">
                      <a class="nav-link" href="{{ path('app_register') }}" ><span class="bi bi-card-text"></span> Register</a>
                  </li>
                  {% endif %}
              </ul>
          </div>
      </div>
  </nav>
